# macOS / Windows / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Node (if you ever add a build step)
node_modules/
dist/
.cache/

# Cloudflare Worker source kept locally for copy/paste into the dashboard.
# The actual deployed Worker code lives in the Cloudflare dashboard
# (Workers & Pages -> bpleon-quotes), not in this repo.
worker-source.js

# Stray edit-recovery temp files (safe to delete from Windows).
*.broken
*.broken2
*.head
*.tail
*.tmp
.test-write

# Drafts (markdown sources, scheduling notes) — local only.
drafts/
