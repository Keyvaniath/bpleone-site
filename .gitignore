# macOS / Windows / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Node (if you ever add a build step)
node_modules/
dist/
.cache/

# Cloudflare Worker source kept locally for copy/paste into the dashboard.
# The actual deployed Worker code lives in the Cloudflare dashboard
# (Workers & Pages -> bpleon-quotes), not in this repo.
worker-source.js

# Stray edit-recovery temp files (safe to delete from Windows).
*.broken
*.broken2
*.head
*.tail
*.tmp
.test-write

# Drafts (markdown sources, scheduling notes) — local only.
drafts/
_goldman_prep/

# Toolkit preview JPGs (auto-generated for QA, not for deployment)
assets/toolkit/*_preview-*.jpg
assets/toolkit/*.~lock.*

# Editor lock files
.~lock.*

# Local-only internal docs (truth tables, handoffs, process notes). These hold
# pre-publication numbers + internal notes — never ship them to the public site.
# (HANDOFF*.md + SUBSCRIBER_IMPORT.md were tracked before this convention and
# were live at bpleon.com/HANDOFF.md etc.; untracked 2026-06 so they 404.)
MU-VERIFIED-DATA-REFERENCE.md
DELL-RESEARCH-HANDOFF.md
SESSION-HANDOFF-*.md
HANDOFF*.md
SUBSCRIBER_IMPORT.md
